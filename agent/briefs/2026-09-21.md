# Crypto Microstructure — 2026-09-21 [flow]

**Council: applied** — directive crypto-microstructure 2026-09-18. Both KEEPs carried: the
unresolvable-row count is remeasured and disclosed below rather than left inside
`scoreboard.py`'s `pending`, and every figure here carries its DAY denominator and the
barred-from-trading arithmetic. The NEW THREAD (§25) is answered, and the directive's premise
about `crypto-rotate.sh` turns out to be stale — see below.

**Data state.** Latest recorded session `research/data/BTC-USD_2026-09-21.csv`, 21.1 MB,
**429,408 trade rows**, last written 08:36 PT — collector alive, nothing stale.
`backtest_log.txt` tail carries only its legend, no new verdict line this pass; the sub-second
honest gate is the senior study and nothing here contradicts it.

**Flow read.** Session **OFI +0.181** (buy 4,998.94 BTC / sell 3,466.45 BTC of 8,465.39 total),
last trade 85,890.00. That would have tripped the old |OFI| ≥ 0.10 trigger. **No call:** the
1-day unit is retired under CRYP-002 and stays retired — this lab's data speaks to one minute,
not one day. Ledger: 4 rows, 0 pending, 0 due.

**Scoreboard — one scoreboard, three clocks, days as the denominator (§4).**
- **1m:** 44,005 resolved / **41 days** · hit 50.0% vs base 49.8% · skill −0.0135 · **resolution 0.0001** · reliability 0.0027
- **5m:** 34,187 resolved / **32 days** · hit 49.9% vs base 49.5% · skill −0.0361 · resolution 0.0001
- **15m:** 33,843 resolved / **32 days** · hit **48.7% vs base 50.3%** · skill −0.0876 · resolution 0.0003
Reliability says the stated probabilities land near their outcomes; **resolution 0.0001 says the
book has no discrimination at all.** That is the whole result, and it must never be reported
without both halves. **+0.05 bps per trade against a 60 bps Coinbase retail taker fee —
about 1/1,183rd of the cost. Barred from trading.**

**Forecast.** Resolved the 09-18 row → **YES (1)**: 2026-09-19 UTC ran **51.9207%** (419 right
of 807 scored, 14 ties excluded as registered), clearing 51.00% by 0.92pp; 807 ≥ 200 so the
void rule did not bind. Filed at 0.33, so it came in on the wrong side — **what a calibrated
0.33 does one time in three.** Late by the sweep's weekday cadence against a 7-day instrument;
declared, not deferred. Filed today: **BTC · target day 2026-09-22 UTC · hit rate exceeds
51.00% · p=0.33**, the recomputed base rate (**13 of 39 complete ≥200-row days**, 09-13 still
the single exclusion at 135). **2026-09-21 UTC is ~65% elapsed and visible (886 scored, 48.646%),
so it is not a target** — a threshold question on a day I can half-read is a leak (§8).
**Lost filing declared:** no row ever named **2026-09-20 UTC**; Sundays go unnamed because the
sweep runs weekdays. Not back-filled. **CAL-001:** `calibrate.py crypto-microstructure 0.33` →
0.33, **[p_cal=0.33]**, unchanged; no bin actionable.

**Unresolvable rows, remeasured (the council's KEEP).** **125 blank rows across 36 days**, of
which exactly **1** (target `2026-09-21T15:38`) is genuinely pending and **124 are permanently
un-priced** — up from the 122 measured on 09-18. `scoreboard.py:32` still publishes all of them
as `pending`. Counted and disclosed; **it changes no outcome**, and
`agent/PROPOSAL_unresolvable_rows.md` stays frozen for Anupam.

**Firm Brain §25 — answered, and the estate-level fact is worse than the directive assumed.**
`crypto-rotate.sh`'s silent git line **was already fixed on 2026-09-20** and now reads the
return code and exits 1 loud. But measured this run, same repo, three environments:
attended shell **rc=0** · `env -i PATH=/usr/bin:/bin:/usr/sbin:/sbin sh -c` **rc=69, Xcode
licence gate** · `env -i … /bin/zsh -c` **rc=0**. **ENV-001 is not fixed at the OS level** — the
cover is `~/.zshenv` setting `DEVELOPER_DIR`, which `zsh` sources even non-interactively. My
three collectors' plists carry **no `EnvironmentVariables` at all** and are protected purely
because their `ProgramArguments` begin `/bin/zsh`. Across all 26 `com.anupam` plists: **17 launch
through zsh and inherit the fix by accident of their launcher, 6 launch a bare
`/opt/anaconda3/bin/python` and inherit nothing, 1 launches `/bin/bash` which does not read
`~/.zshenv` either.** Only `track-score` among the bare-python jobs calls git, and its plist sets
`DEVELOPER_DIR` explicitly — so there is **no uncovered exposure today**, covered by two
mechanisms, one of them invisible. **Candidate new mechanism for the brain: an environment fix
that lives in a dotfile is scoped to the LAUNCHER, not to the job, and rewriting a zsh-wrapped
job as a bare binary silently un-fixes it.** Reported, not patched (REG-PP-001).
