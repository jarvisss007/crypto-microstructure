# Crypto Microstructure — 2026-08-04 [flow]

## Data state
Collector is **alive and recovered** — the 08-04 session file is being written
right now (last trade 2026-08-05 03:49 UTC, 90,619 trades, ~20.8h of coverage).
But the previous session is a **hole**: `BTC-USD_2026-08-03.csv` spans only
**03:18–06:59 UTC on 08-04**, i.e. **3.7 of 24 hours** — ~20.3h lost. This is
the tail of the same outage flagged on 08-03; the collector came back, the
session it dropped is gone for good.

## Flow read
- Last **complete** session (08-03, 84% missing): OFI **−0.024** on 58,599
  trades.
- In-progress session (08-04, ~20.8h): OFI **+0.061** on 90,619 trades.
- Neither is anywhere near the ±0.10 trigger, and the sign flips between them.

Honest gate (`backtest_log.txt`, auto-run 2026-08-04 20:12, 88,246 trades /
409 min, 60 bps/trade): **no signal is net-positive after costs.** `book_imb`
carries the stronger association (IC 0.191 at 5s, p=0.002) and `ofi` a weaker
one (IC 0.106 at 5s), and both are net ≈ −60 bps at every horizon — the gross
edge is ~0.08 bps against a 60 bps cost. The sub-second study is the senior
result and this brief does not contradict it.

## Today's call
**No call — flow balanced.** |OFI| is 0.024 on the last complete session, far
below the fixed 0.10 threshold. Separately, that session is 84% missing, so
even a triggering reading would not have been a session-wide imbalance. The
threshold is not being tuned; both routes independently say stand aside.

## Scorecard
**1/2 (50%), 0 pending.** n=2 carries no information about the hypothesis —
one call right in each direction is exactly what a coin flip looks like. The
20-call bar for the coin-flip test is a long way off.

Forecast logged (mandatory, not a call): BTC-USD UTC daily close above
64,050.51 on 2026-08-08, p=0.50.
