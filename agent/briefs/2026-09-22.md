# Crypto Microstructure — 2026-09-22 [flow]

## Data state
`research/data/BTC-USD_2026-09-22.csv` written 08:40 PT, **258,620 trade rows**. Collector alive.
Minute instrument current: `minute_forecasts.csv` last day **2026-09-22**, 617 rows scored so far.

## Flow read
**Session OFI −0.0464** (buyVol 1792.17 / sellVol 1966.76 of 3758.93 total). Last trade 86,366.98.
`backtest_log.txt` senior verdict, unchanged and never contradicted: **no signal is net-positive
after costs — the expected, honest result.**

## Today's call
**No call. Flow balanced — |OFI| 0.0464 is below the 0.10 trigger**, and the 1-day unit is retired by
CRYP-002 regardless. The threshold was not touched.

## Scored today
**Nothing, correctly.** The one open forecast (filed 09-21) names the **2026-09-22 UTC** day, which is
still running — 617 scored, 314 right, 50.89% at 15:40 UTC. Under SCHED-001 this book resolves
check_date+1 as a **standing condition graded PASS by design**; it is not a deferral and not a
catch-up miss. It resolves tomorrow, untouched.

## Scoreboard — one scoreboard, three clocks, DAYS as the denominator
| horizon | resolved | days | hit | base | edge | skill | resolution |
|---|---|---|---|---|---|---|---|
| 1m | 45,057 | **42** | 50.03% | 49.79% | −0.17pp | −0.0135 | **0.0000** |
| 5m | 35,220 | **33** | 49.9% | 49.5% | +0.4pp | −0.0363 | 0.0001 |
| 15m | 34,829 | **33** | 48.8% | 50.2% | −1.4pp | −0.0871 | 0.0003 |

Resolution 0.0000 means the book forecasts its own base rate every time — under Brain §14 the skill
figure **is not evidence about the signal at any n**. The 15m book is both miscalibrated *and*
non-discriminating, the worse of the two readings. §4: 45,057 rows over 42 days is **42 observations
of regime**, not 45,057.
**BARRED FROM TRADING:** +0.05 bps per trade against a 60 bps Coinbase retail taker fee — about
**1/1,183rd** of the cost.

## Forecast filed (1 of 1)
**Minute-forecaster directional hit rate on 2026-09-23 UTC scored rows exceeds 51.00% — p = 0.33**
`[p_cal=0.33]`. Reference class recomputed this run: 13 of **40** complete UTC days ≥200 rows cleared
51.00% → base **0.325** (was 13/39 = 0.3333; 09-21 completed at 49.281% on 1,321 rows, below). Target
is 09-23, not today, because today is already half-readable (§8). Forecast book: n=33 resolved,
Brier 0.2602 vs climatology 0.2443, **skill −0.0653, no skill**.

## COUNCIL FIX — plist census re-run BY NEED; my 09-21 claim was wrong
I had checked git usage among the *bare-python* jobs only and wrote "no uncovered exposure today".
Correct method: list the jobs that **call git**, then ask which the fix reaches. **24 jobs; 7 call git.**
**COVERED (6):** crypto-rotate, gdrive-backup, track-c, track-d, value-forward — all launch
`/bin/zsh`, which sources `~/.zshenv`; plus **track-score**, bare python but carrying
`DEVELOPER_DIR` explicitly in its plist.
**NOT COVERED (1): `com.anupam.tradinghub-refresh`** → `/bin/bash /Users/anupampatil/strategy-lab/refresh_all.sh`,
empty `EnvironmentVariables`, calls git every 30 minutes. `/bin/bash` does not source `~/.zshenv`.
`grep -c 'not agreed to the Xcode license' ~/strategy-lab/refresh.log` read **157** when the council
wrote the FIX and reads **252** now — **+95 in a day**, failing continuously.
**It fails closed and loud:** `refresh_all.sh:56` probes `git rev-parse --git-dir` and logs
`SKIPPED — git cannot run here` rather than guessing. Nothing is corrupted. The cost is that
**strategy-lab's cloud integration has not pulled for five days** while the rest of the refresh kept
publishing normally — a job 90% working is the hardest kind to notice.
**Nothing was changed** — plist, launcher and `~/.zshenv` untouched (REG-PP-001).
